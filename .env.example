# Linear OAuth application (Linear → Settings → API → OAuth applications)
LINEAR_CLIENT_ID=
LINEAR_CLIENT_SECRET=

# Random secret used to encrypt the session cookie. Generate with: openssl rand -hex 32
SESSION_SECRET=

# Public URL of the app. Optional locally (defaults to the request origin), required behind a proxy or on Vercel.
# APP_URL=https://your-domain.com
